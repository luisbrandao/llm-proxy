# Secrets referenced by config.yaml via ${VAR} interpolation.
DEEPSEEK_API_KEY=sk-your-deepseek-key
OPENROUTER_API_KEY=sk-your-openrouter-key

# Path to the YAML config inside the container (set by docker-compose).
# CONFIG_PATH=/app/config.yaml
